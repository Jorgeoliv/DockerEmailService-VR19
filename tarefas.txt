No lado do auth:

-> Guardar o token na BD (hash) depois de ser feito um login com sucesso. Só depois fazemos o redirect (cagativo)
-> Fazer o /info <= Agora está hardcoded a devolver o "qq" mas temos de procurar na Base de Dados
-> Uma página index para fazer login ou registo
-> Logout (base de dados e apagar o token)

Compositor:

-> Falta ligar ao serviço de email (ver em conjunto, com pré-pesquisas em casa, no carro em todo o lado)
-> Ligar o email recebido para a view "compositor"
-> Falta a imagem do compositor (Dockerfile)
-> Logout (redirecionar para o auth)

Serviço de Email:

-> Ver qual e como é que podemos utilizar
